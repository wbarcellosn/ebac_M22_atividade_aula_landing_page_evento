{
  "name": "landing-page-evento",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "cleanup": "rmdir /q /s .parcel-cache dist || echo 'no cached files'",
    "dev": "npm run cleanup && parcel src/index.html src/styles/main.scss src/scripts/main.js",
    "build": "parcel build src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.10.0",
    "parcel": "^2.10.0",
    "sharp": "^0.31.3"
  }
}
